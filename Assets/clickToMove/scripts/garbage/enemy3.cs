using UnityEngine;
using System.Collections;

/*
 * Brian Kang
 * 10-07-13
 * 
 * Third type of enemy. Patrols in a circle of random size
 * Too complicated to make. Scrapped but original code is still here for later reference
 */
public class enemy3 : MonoBehaviour {

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
